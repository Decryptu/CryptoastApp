export const API_CONFIG = {
	BASE_URL: "http://192.168.1.28:3000/api", // Use the local IP or public IP as 'localhost' doesn't work with Android emulators/devices
	ITEMS_PER_PAGE: 10 as number,
} as const;
